- content_for :head do
  %title #{@company.name} details

%h4 Compnay Details
%div{:class => "btn-group"}
  %button{:type => "button", :class => "btn btn-info dropdown-toggle", data: {toggle: "dropdown"}}
    Update
    %span{:class => "caret"}
    
  %ul{:class => "dropdown-menu", :role => "menu"}
    %li= link_to "Edit", edit_company_path(@company)
    %li= link_to "Delete", @company, :method => "delete", data: { confirm: "Are You sure?" }

%hr
%p
  %strong Company Name:
  = @company.name
  
%br
- content_for :aside do
  = render "sidebar"