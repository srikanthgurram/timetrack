- content_for :head do
  %title #{@project.name} Details

%h4 Project Details
%p= link_to "Edit Project", edit_project_path(@project)
%p
  %ul
    %li
      %strong Project Name: 
      #{@project.name}
    %li
      %strong Default Rate: 
      #{@project.default_rate}
    %li
      %strong All the work done: 
      #{link_to "Details", {:controller => 'projects', :action => 'works'}}

- content_for(:aside) do
  = render "sidebar"